# === NovaOS Environment Template ===
# Copy to .env for local runs. In Render, set REAL values here:
# Dashboard → Services → novaos-dashboard → Environment
# Do NOT commit real secrets to the repo.

# -----------------------------
# Core AI
# -----------------------------
OPENAI_API_KEY=             # required for LLM responses in the dashboard

# If you want Grok via xAI:
XAI_PROVIDER=grok
GROK_API_KEY=

# If you keep LangGraph in the loop (optional):
LANGGRAPH_API_KEY=

# Optional: Perplexity for research agent (if used by PerplexityFetcherAgent)
PERPLEXITY_API_KEY=

# -----------------------------
# Memory / Persistence
# -----------------------------
# External Postgres URL from Render (must include sslmode=require)
# Example pattern:
# postgresql://<user>:<password>@<host>/<db>?sslmode=require
DATABASE_URL=

# Enable/disable memory-backed features in the dashboard
MEMORY_ENABLED=1

# -----------------------------
# Cache / PubSub
# -----------------------------
# Local default is fine; set a managed Redis URL if you deploy one
REDIS_URL=redis://localhost:6379

# -----------------------------
# Commerce / Integrations (optional)
# -----------------------------
LEMON_SQUEEZY_API_KEY=
SHOPIFY_API_KEY=
BASEROW_API_TOKEN=
DROPBOX_TOKEN=

# -----------------------------
# App behavior / misc
# -----------------------------
RENDER=1                    # set to 1 on Render; unset or 0 locally if you want
AGENT_LOOP_INTERVAL=5       # seconds between background ticks
LOG_LEVEL=info              # info | debug

# Streamlit telemetry off (optional)
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
